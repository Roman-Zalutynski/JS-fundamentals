// ! module 4 lesson 2
// functions - always returns something. So we can work with returned value 
//function
function sum(a, b) {
   return a + b;
}
console.log(sum(3, 5));

// procedure - just do something inside the funtion and doesn't return any value;
function sum2(a, b) {
   console.log(a + b);
}
